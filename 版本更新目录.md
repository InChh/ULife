## 版本更新目录

### 2025-12-12（cyk-fully-implemented-v4-Scheduled-2025-12-12）

- **AI 聊天助手上线**：
  - 新增 `AIChatViewController` 聊天界面与悬浮球入口（`AIAssistantManager`），支持会话续写与刷新上下文。
  - 基于 Protobuf 的 `/v1/proto/ai/chat`、`/v1/proto/ai/history` 接口，实现与后端大模型服务通信。

- **AI 工具接入**：
  - `modules/ai/tools.rs` 新增三类工具：
    - 活动查询工具（查询 `activities` 表，支持按时间范围筛选并返回精简活动信息）。
    - 天气查询工具（接入 Open‑Meteo API，根据城市名获取当日天气）。
    - 论坛摘要工具（复用 `BbsService::get_posts`，拉取近期高热度帖子供 AI 总结）。
  - `AIService::chat` 根据关键词自动选择调用活动/天气/论坛工具，并在模型输出为空时用工具结果生成兜底回答。

- **每日校园简报**：
  - 后端新增定时任务 `AIService::generate_daily_digest`，周期性汇总当天活动与论坛数据，让大模型生成 100 字内的“今日校园简报”，写入 `system_digest` 会话。
  - 新增接口 `GET /v1/ai/daily_digest`，返回最近一条简报（日期、内容、生成时间）。
  - iOS 端新增 `DailyDigestManager`，在 App 启动/回到前台时拉取简报，并通过本地通知向用户推送（每天一次，Debug 可配置高频测试）。

- **其它改动**：
  - 修复 UTF‑8 截断导致的 panic，对会话标题、帖子摘要等统一改为按字符截断。
  - 优化网络错误与日志输出，提升 AI 聊天与工具调用的可观测性与稳定性。
