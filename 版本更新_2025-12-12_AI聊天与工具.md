## 版本更新说明（2025-12-12）

本次版本在原有活动、论坛、课表等功能基础上，重点引入 **AI 聊天助手** 以及与校园数据/外部服务打通的能力。

---

## 一、AI 聊天助手模块

- **iOS 端新增 `AIChatViewController`**：
  - 通过 `AIAssistantManager` 提供的悬浮球入口打开，支持查看历史消息、连续对话。
  - 首次进入时自动发起“预热”请求，建立会话并获取欢迎语。
  - 导航栏新增“刷新”按钮，可清空当前会话上下文和本地 `conversation_id`，重新开始新对话。
- **Protobuf 接入**：
  - 新增协议文件 `Protocol/ai.proto`，并在 iOS 侧生成 `ai.pb.swift`，后端生成 `campus.ai.rs`。
  - 客户端与后端通过 `/v1/proto/ai/chat` 和 `/v1/proto/ai/history` 进行二进制 Protobuf 通信。
  - 支持会话 ID 持久化与历史消息拉取，实现“续聊”和跨设备继续会话。

---

## 二、后端 AI 模块增强

- **统一 AI 服务入口 `AIService::chat`**：
  - 负责创建/校验 AI 会话（`ai_conversations` 表），将用户与助手消息写入 `ai_messages` 表。
  - 自动构建完整的对话历史并追加固定的 System 提示，调用上游大模型（SiliconFlow OpenAI 兼容 API）。
- **新增工具模块 `modules/ai/tools.rs`**：
  - **活动查询工具**：
    - 基于 `activities` 表，按时间范围（7 天 / 30 天 / all）和数量限制查询近期校园活动。
    - 返回精简结构（标题、地点、开始时间、名额等），供大模型做“活动推荐/总结”。
  - **天气工具**：
    - 使用 Open‑Meteo Geocoding + Weather API，根据用户问题中的城市名（如“北京”“安阳”）解析经纬度，并获取当日天气。
    - 返回地点、日期、温度及中文天气描述，既可由大模型使用，也可在模型无输出时由服务端兜底生成回答。
  - **论坛摘要工具**：
    - 复用 `BbsService::get_posts`，按热度和时间从 `bbs_posts` 中拉取若干条最新帖子。
    - 返回标题、板块名、摘要、点赞/评论数等信息，支持“今天校园论坛在讨论什么”类问题。
- **工具编排逻辑**：
  - `AIService::chat` 会读取本轮用户的最后一条消息，根据关键词（天气/活动/论坛）决定是否调用对应工具。
  - 工具结果以 JSON 文本的形式追加为 System 消息注入给大模型，作为回答时可以参考的数据源。
  - 若模型在有工具数据的情况下仍返回空内容，服务端会：
    - 对天气问题：直接用 `WeatherInfo` 结构组装自然语言回答。
    - 对活动问题：用活动列表生成带编号的活动推荐/总结文案。
    - 对论坛问题：用帖子列表生成“近期热门帖话题”概览，包含板块与互动数据。

---

## 三、客户端体验优化

- 当 AI 返回空字符串时，前端不再展示“（无内容）”，而是提示“这次我没有生成有效的回答，请稍后再试一次。” 或使用服务端兜底生成的结果。
- 刷新按钮行为统一：清空本地消息列表和会话 ID，并自动重新拉起欢迎语，确保新会话不会受到旧上下文干扰。

---

## 四、稳定性与兼容性修复

- 修复多处 UTF‑8 字符截断问题：会话标题、论坛摘要等统一使用 `chars().take(n)`，避免在中文/emoji 上触发 `is_char_boundary` 断言 panic。
- 后端工具调用（活动/天气/论坛）失败时仅记录 `warn` 日志，不再导致整个聊天接口返回 500 或中断连接。
- 网络层对 Protobuf/JSON 响应增加更多日志与错误处理，便于排查线上问题。
